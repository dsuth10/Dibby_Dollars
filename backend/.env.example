# Dibby Dollars Backend - Environment variables
# Copy to .env and set values. Do not commit .env.

# Secret key for session signing (use a long random string in production)
SECRET_KEY=your-secret-key-change-in-production

# Database URL (default: SQLite in instance/dibby_dollars.db)
# DATABASE_URL=sqlite:///instance/dibby_dollars.db
# For PostgreSQL: DATABASE_URL=postgresql://user:pass@localhost/dibby_dollars

# Set to "production" in production; leave unset or "development" for dev
# FLASK_ENV=development

# Set to "testing" when running pytest (disables scheduler)
# FLASK_ENV=testing
