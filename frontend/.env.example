# Dibby Dollars Frontend - Environment variables
# Copy to .env.local or .env and set values. Do not commit .env files.

# Backend API base URL (no trailing slash)
# Development (default if unset):
# VITE_API_URL=http://localhost:5000/api
# Production: set to your deployed backend URL
# VITE_API_URL=https://api.yourschool.edu/dibby-dollars/api
